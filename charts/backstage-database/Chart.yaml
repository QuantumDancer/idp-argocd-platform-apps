apiVersion: v2
name: backstage-database
description: PostgreSQL DB for Backstage
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: cluster
    version: 0.6.0
    repository: https://cloudnative-pg.github.io/charts
