apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: cilium
spec:
  # Cilium v1.17+ no longer auto-creates this GatewayClass; it must be explicitly declared.
  controllerName: io.cilium/gateway-controller
