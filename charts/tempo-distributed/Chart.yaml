apiVersion: v2
name: tempo-distributed
description: Grafana Tempo in distributed (microservices) mode with custom configuration
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: tempo-distributed
    version: 3.4.0
    repository: https://grafana-community.github.io/helm-charts
