apiVersion: v2
name: loki
description: Longhorn distributed block storage system for Kubernetes with custom configuration.
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: longhorn
    version: 1.11.0
    repository: https://charts.longhorn.io
