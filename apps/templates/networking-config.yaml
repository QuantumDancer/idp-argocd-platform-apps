# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/argoproj.io/application_v1alpha1.json
{{- if .Values.networkingConfig.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: networking-config
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1" # Requires external-secrets-operator and gateway-api-crds
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform

  source:
    repoURL: {{ include "platform-apps.repoURL" . }}
    targetRevision: {{ include "platform-apps.targetRevision" . }}
    path: charts/networking-config
    helm:
      releaseName: networking-config
      valueFiles:
        - values.yaml
        - environments/{{ .Values.environment }}.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: argocd

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
{{- end }}
