apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: monitoring-daemonset
value: 1000000
globalDefault: false
description: "Priority class for monitoring DaemonSet pods to ensure they are scheduled on every node."
