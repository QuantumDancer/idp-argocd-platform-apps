environment: development

repos:
  platformApps:
    targetRevision: development
  backstage:
    targetRevision: development
  crossplaneCompositions:
    targetRevision: development
  userApps:
    targetRevision: development

# Dex brokers IAM Identity Center SAML -> OIDC for Grafana SSO (AWS only).
dex:
  enabled: true
